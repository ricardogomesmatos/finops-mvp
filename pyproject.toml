[tool.uv.workspace]
members = ["libs/*", "pipelines/*"]

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
    "ruff>=0.8,<1",
    "pre-commit>=4.0,<5",
    "mypy>=1.13,<2",
]

[tool.pytest.ini_options]
testpaths = ["libs", "pipelines"]
pythonpath = ["."]
